# Node-Sequelize-tutorial

# Install Sequelize and DB Driver
 npm install --save sequelize
 npm install --save pg pg-hstore # Postgres

# Create migration
 npm install --save-dev sequelize-cli
 npx sequelize-cli init

